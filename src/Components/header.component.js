/*import React, {useState} from 'react';


LinkChanged: Boolean = false;

function Header(){

    const [counter, setCounter] = useState(0);

    function trocaValor(){
       setCounter(counter + 1);
    }


     return (
        <header>
    
            <nav id="form-herader">
               <from>
                   <input type = "text" placeholder = "Teste" />
               </from>
            <button onClick={trocaValor}>Troca Valor</button>
            <h1>{counter}</h1>
            </nav>
        </header>
    );
}

export default Header;*/